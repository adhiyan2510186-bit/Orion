# Verified working on Python 3.14.7 (Windows x64), all prebuilt wheels - no compiler needed.
# Pinned to the versions this build was tested against.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0

# Data. pandas 3.x and numpy 2.x are major versions: copy-on-write is the default,
# so chained assignment silently does nothing. Verify APIs rather than recalling
# pandas 1.x idioms.
pandas==3.0.5
numpy==2.5.3
xarray==2026.7.0
netCDF4==1.7.4
pyarrow==25.0.1

# Dev
pytest==9.1.1
pytest-asyncio==1.3.0
httpx==0.28.1
ruff==0.15.1
